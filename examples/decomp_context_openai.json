{
  "category": "API wrapper",
  "strategy": "Replace with raw HTTP calls using httpx: create a lightweight client that takes api_key, base_url, timeout and calls {base_url}/chat/completions via httpx.post(). Remove inheritance from openai exception classes — define independent exceptions. Replace openai._models.BaseModel usage with pydantic BaseModel. Replace openai.types.completion_usage.* imports with custom equivalents.",
  "functions_to_replace": [
    "openai.OpenAI() constructor",
    "client.chat.completions.create() and with_raw_response variant",
    "All openai exception base classes",
    "openai._models.BaseModel (alias for OpenAIObject)",
    "openai.types.completion_usage.CompletionTokensDetails, CompletionUsage, PromptTokensDetails"
  ],
  "reference_material": ".slash_diy/reference/openai/",
  "acceptable_sub_dependencies": ["httpx", "pydantic"]
}
