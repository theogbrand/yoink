# Orchestration Flow

Auto-generated by `scripts/orchestration-linter.py`. Do not edit manually.

## Agents

- **decomp-evaluator**(library_name, package_name) → decision, reasoning, category, strategy, functions_to_replace, reference_material, acceptable_sub_dependencies: Evaluate whether a dependency should be kept or decomposed. Use during dependency decomposition (phase 1) to assess each library in the queue.
- **decomp-implementer**(top_package, sub_package, category, strategy, functions_to_replace, reference_material, acceptable_sub_dependencies, max_iterations) → completion_promise, what_was_done, new_imports: Implement or replace a dependency in yoink_<package>/ based on a decomposition evaluation. Use during dependency decomposition (phase 1) after evaluation.
- **test-discoverer**(package_name, target_function) → files_found, files_copied, summary: Search reference test suite for tests relevant to a target function. Use during test curation (phase 0) to discover existing tests from the original library.
- **test-generator**(package_name, target_function) → tests_written, test_files, summary: Generate focused pytest tests for a target function by studying reference implementation. Use during test curation (phase 0) after test discovery.

## Skill Flows

```
/setup --url GITHUB_URL [--package PACKAGE_NAME]
  (no orchestration steps detected)

/test-curate PROMPT --package PACKAGE_NAME
  1. Discover relevant tests
     ├─ `--skip-discovery` is present → skip to step 2
     └─ agent: test-discoverer(package_name, target_function) → files_found, files_copied, summary
  2. Generate focused tests
     └─ agent: test-generator(package_name, target_function) → tests_written, test_files, summary
  3. Validate tests against the real library
     ├─ runs: pytest
     └─ zero tests survive → abort
  4. Rewrite imports
     └─ runs: ${CLAUDE_SKILL_DIR}/scripts/rewrite_imports.py
  5. Sanity check
     └─ runs: ${CLAUDE_PLUGIN_ROOT}/scripts/run_tests.py

/decompose --package PACKAGE_NAME
  [Decomposition Loop]
  ┌─ loop
  │ 1. Dequeue
  │    ├─ runs: ${CLAUDE_SKILL_DIR}/scripts/decomp.py dequeue
  │    └─ queue is empty → decomposition complete, remove the real library and stop
  │ 2. Evaluate
  │    ├─ agent: decomp-evaluator(library_name, package_name) → decision, reasoning, category, strategy, functions_to_replace, reference_material, acceptable_sub_dependencies
  │    ├─ Keep → go back to step 1
  │    └─ Decompose → continue to step 3
  │ 3. Prepare the sub-package for the implementer agent
  │    ├─ runs: pytest
  │    └─ runs: ${CLAUDE_SKILL_DIR}/scripts/activate-inner-yoink-loop.sh
  │ 4. Implement & Validate
  │    └─ agent: decomp-implementer(top_package, sub_package, category, strategy, functions_to_replace, reference_material, acceptable_sub_dependencies, max_iterations) → completion_promise, what_was_done, new_imports
  │ 5. Enqueue new dependencies
  │    └─ runs: ${CLAUDE_SKILL_DIR}/scripts/decomp.py enqueue
  └─ back to step 1

/yoink PROMPT [--url GITHUB_URL] [--package PACKAGE_NAME] [--skip-discovery]
  1. Setup → /setup
     └─ runs: ${CLAUDE_SKILL_DIR}/scripts/setup.sh
  2. Test Curation → /test-curate
     1. Discover relevant tests
        ├─ `--skip-discovery` is present → skip to step 2
        └─ agent: test-discoverer(package_name, target_function) → files_found, files_copied, summary
     2. Generate focused tests
        └─ agent: test-generator(package_name, target_function) → tests_written, test_files, summary
     3. Validate tests against the real library
        ├─ runs: pytest
        └─ zero tests survive → abort
     4. Rewrite imports
        └─ runs: ${CLAUDE_SKILL_DIR}/scripts/rewrite_imports.py
     5. Sanity check
        └─ runs: ${CLAUDE_PLUGIN_ROOT}/scripts/run_tests.py
  3. Dependency Decomposition → /decompose
     [Decomposition Loop]
     ┌─ loop
     │ 1. Dequeue
     │    ├─ runs: ${CLAUDE_SKILL_DIR}/scripts/decomp.py dequeue
     │    └─ queue is empty → decomposition complete, remove the real library and stop
     │ 2. Evaluate
     │    ├─ agent: decomp-evaluator(library_name, package_name) → decision, reasoning, category, strategy, functions_to_replace, reference_material, acceptable_sub_dependencies
     │    ├─ Keep → go back to step 1
     │    └─ Decompose → continue to step 3
     │ 3. Prepare the sub-package for the implementer agent
     │    ├─ runs: pytest
     │    └─ runs: ${CLAUDE_SKILL_DIR}/scripts/activate-inner-yoink-loop.sh
     │ 4. Implement & Validate
     │    └─ agent: decomp-implementer(top_package, sub_package, category, strategy, functions_to_replace, reference_material, acceptable_sub_dependencies, max_iterations) → completion_promise, what_was_done, new_imports
     │ 5. Enqueue new dependencies
     │    └─ runs: ${CLAUDE_SKILL_DIR}/scripts/decomp.py enqueue
     └─ back to step 1

```
