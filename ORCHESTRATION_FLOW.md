# Orchestration Flow

Auto-generated by `scripts/orchestration-linter.py`. Do not edit manually.

## Agents

- **decomp-evaluator**: Evaluate whether a dependency should be kept or decomposed. Use during dependency decomposition (phase 1) to assess each library in the queue.
- **decomp-implementer**: Implement or replace a dependency in diy_<package>/ based on a decomposition evaluation. Use during dependency decomposition (phase 1) after evaluation.
- **test-discoverer**: Search reference test suite for tests relevant to a target function. Use during test curation (phase 0) to discover existing tests from the original library.
- **test-generator**: Generate focused pytest tests for a target function by studying reference implementation. Use during test curation (phase 0) after test discovery.

## Command Flows

```
/test-curate PROMPT --package PACKAGE_NAME
  1. Discover relevant tests
     └─ agent: test-discoverer
  2. Generate focused tests
     └─ agent: test-generator
  3. Validate tests against the real library
     ├─ runs: pytest
     └─ zero tests survive → abort
  4. Rewrite imports
     └─ runs: rewrite_imports.py
  5. Sanity check
     └─ runs: run_tests.py

/decompose --package PACKAGE_NAME
  [Decomposition Loop]
  ┌─ loop
  │ 1. Dequeue
  │    ├─ runs: scripts/decomp.py dequeue
  │    └─ queue is empty → stop, decomposition complete
  │ 2. Evaluate
  │    ├─ agent: decomp-evaluator
  │    ├─ Keep → go back to step 1
  │    └─ Decompose → continue to step 3
  │ 3. Implement & Validate
  │    └─ agent: decomp-implementer
  │ 4. Enqueue new dependencies
  │    └─ runs: scripts/decomp.py enqueue
  └─ back to step 1

/diy-decomp PROMPT [--url GITHUB_URL] [--package PACKAGE_NAME]
  [Phase 0: Test Curation]
  1. Discover relevant tests
     └─ agent: test-discoverer
  2. Generate focused tests
     └─ agent: test-generator
  3. Validate tests against the real library
     ├─ runs: pytest
     └─ zero tests survive → abort
  4. Rewrite imports
     └─ runs: rewrite_imports.py
  5. Sanity check
     └─ runs: run_tests.py
  [Phase 1: Dependency Decomposition]
  ┌─ loop
  │ 1. Dequeue
  │    ├─ runs: scripts/decomp.py dequeue
  │    └─ queue is empty → stop, decomposition complete
  │ 2. Evaluate
  │    ├─ agent: decomp-evaluator
  │    ├─ Keep → go back to step 1
  │    └─ Decompose → continue to step 3
  │ 3. Implement
  │    └─ agent: decomp-implementer
  │ 4. Enqueue
  │    └─ runs: scripts/decomp.py enqueue
  └─ back to step 1

/diy-loop PROMPT [--url GITHUB_URL] [--max-iterations N] [--completion-promise TEXT]
  (no orchestration steps detected)

```
