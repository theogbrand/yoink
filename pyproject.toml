[project]
name = "slash-diy"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]

[tool.pytest.ini_options]
python_files = ["test_*.py", "disc_test_*.py"]
asyncio_mode = "auto"
